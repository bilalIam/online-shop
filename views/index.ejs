<%- include("includes/head.ejs")%>
</head>
<body>
<%- include("includes/nav.ejs")%>

    <h1>List of products</h1>
    <% if(products.length === 0) { %>
        <h1>nema nista bajo</h1>
    <% } else { %>
        <ul>
            <% for(const product of products) { %>
                <%- include("includes/product.ejs", {product}) %>
            <% } %>
        </ul>
    <% } %>
</body>
</html>